// Вибірковий import: тягне лише square і Vector2 (з методом length())
// з modules/lib_selective.arx, залишаючи cube і unused неімпортованими.
import "modules/lib_selective.arx" { square, Vector2 }

func main() {
    print("square(5) = " + toString(square(5)))

    var v = Vector2 { x: 3, y: 4 }
    print("довжина вектора = " + toString(v.length()))
}
