// Перевіряє, що assertEqual дійсно падає на неправильному твердженні
// (навмисна помилка - EXPECT_ERROR, тестує сам тестовий інструмент).
import "../lib/testing.arx" { assertEqual }

func main() {
    assertEqual(2 + 2, 5, "навмисно неправильне твердження")
}
